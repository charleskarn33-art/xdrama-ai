/**
 * Placeholder until Module 2 (Auth & Core Schema) ships the first migration.
 * Regenerate with:
 *   supabase gen types typescript --project-id <project-id> > src/lib/supabase/types.ts
 */
export type Database = Record<string, never>;
